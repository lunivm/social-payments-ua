{
  "name": "social-payments-ua_database",
  "engines": {
    "node": "10.15.x"
  },
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose -f app-release.docker-compose.yml up"
  }
}
